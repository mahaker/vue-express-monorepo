{
  "name": "vue-express-monorepo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev:frontend": "lerna run --scope=frontend-app --stream dev",
    "build:frontend": "lerna run --scope=frontend-app build",
    "dev:server": "lerna run --scope=server-app --stream dev",
    "build:server": "lerna run --scope=server-app build",
    "bootstrap": "lerna bootstrap"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
